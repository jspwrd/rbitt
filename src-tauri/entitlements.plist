<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow outgoing network connections (required for peer connections, trackers, DHT) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Allow incoming network connections (required for accepting peer connections) -->
    <key>com.apple.security.network.server</key>
    <true/>

    <!-- Allow read/write access to user-selected files (for downloading torrents) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Allow read access to Downloads folder -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>
</dict>
</plist>
